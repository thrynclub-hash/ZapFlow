---
phase: new-project-initialization
task: research-complete
total_tasks: 9
status: in_progress
last_updated: 2026-07-05T23:52:29.483Z
---

<current_state>
Rodando o workflow `/gsd:new-project` (unificado: questionamento → pesquisa → requirements → roadmap)
pra planejar a "Fase 2" do ZapFlow — camada leve de mini-CRM (status de contato, painel de consumo,
status de assinatura, import de leads do LinkedIn). Nenhum agente especializado do GSD
(`gsd-codebase-mapper`, `gsd-project-researcher`, `gsd-research-synthesizer`, `gsd-roadmapper`) está
instalado neste ambiente (`.claude/agents/` não existe) — todos os passos que pediam esses agentes
foram adaptados usando `general-purpose` com prompts equivalentes. Isso funcionou bem, mas continuar
precisa da mesma adaptação (não existe `gsd-roadmapper` pra invocar na próxima etapa).

Pausado por aviso de contexto em 83% de uso, ANTES de começar Requirements (etapa 7) e Roadmap
(etapa 8) — que são as duas etapas mais pesadas que faltam.
</current_state>

<completed_work>

- Etapa 1 (Setup): `node .claude/get-shit-done/bin/gsd-tools.cjs init new-project` rodado com cwd em
  `C:\mega-brain-test\projects\ZapFlow`. Detectou brownfield (`is_brownfield: true`).
- Etapa 2 (Brownfield offer): usuário escolheu "Mapear primeiro" → rodei `/gsd:map-codebase` completo
  (4 agentes `general-purpose` em paralelo, mapper_model=haiku) → 7 documentos escritos em
  `.planning/codebase/` (STACK, ARCHITECTURE, STRUCTURE, CONVENTIONS, TESTING, INTEGRATIONS, CONCERNS —
  2966 linhas total). Scan de segredos limpo. Commitado (`docs: map existing codebase`, hash b558a2f).
- Etapa 3 (Deep questioning): feito via conversa direta (não formulário) — usuário confirmou:
  quer chegar perto de/superar Bolten e HelenaCRM; delegou a mim a decisão de ambiente técnico
  isolado; delegou a mim decidir o que entra na Fase 2 após pesquisa; só precisa "ver visualmente
  se está funcionando" — sem setup local pra ele revisar.
- Etapa 4 (PROJECT.md): escrito e commitado (`docs: initialize project`, hash 6d69713). Contém:
  What This Is, Core Value, Requirements Validated (inferido do codebase map — 11 capacidades já
  existentes em produção), Requirements Active (4 hipóteses de Fase 2 — status lifecycle, painel de
  consumo, status de assinatura, import LinkedIn — ainda a confirmar/refinar com requirements
  formais), Out of Scope (Kanban, IA/agentes, inbox multiatendente — são Fase 3/4), Context, Constraints
  (produção intocável, ambiente isolado, revisável só visualmente, respeitar limite anti-bloqueio,
  compliance LinkedIn), Key Decisions (ambiente isolado, uso de GSD, pesquisa real).
- Etapa 5 (Config): não usei o gate interativo (usuário já tinha delegado decisões) — escolhi
  diretamente mode=yolo, depth=standard, parallelization=false, commit_docs=true,
  model_profile=balanced, workflow.research/plan_check/verifier=true. Escrito e commitado
  (`chore: add project config`, hash 335fee0).
- Etapa 6 (Research): rodada completa. 4 agentes `general-purpose` (model=sonnet) em paralelo —
  STACK.md (147 linhas), FEATURES.md (194 linhas, pesquisa real via WebSearch/WebFetch em bolten.io,
  bolten.gitbook.io, helenacrm.com, docs.helena.app, kommo.com), ARCHITECTURE.md (146 linhas),
  PITFALLS.md (140 linhas, leu código real: mp-webhook/index.ts, supabase_plan_billing.sql,
  supabase_planos_limites.sql). Depois sintetizei com 1 agente `general-purpose` adicional →
  SUMMARY.md (65 linhas) em `.planning/research/`. Tudo commitado
  (`docs: research Fase 2 domain...`, hash f8f764f).
</completed_work>

<remaining_work>

- **Etapa 7 (Requirements)**: ainda não iniciada. Ler `.planning/research/FEATURES.md` e
  `.planning/research/SUMMARY.md`, transformar as 4 hipóteses de Active requirements do PROJECT.md
  em REQ-IDs formais categorizados (provavelmente categorias: Status/CRM leve, Consumo, Cobrança,
  Import). A SUMMARY.md já tem a tabela table-stakes/diferencial/anti-feature pronta pra virar
  requirements — não precisa repesquisar, só formalizar. Escrever `.planning/REQUIREMENTS.md` seguindo
  o template em `.claude/get-shit-done/templates/requirements.md`. Como mode=yolo, não precisa gate de
  aprovação do usuário — pode gerar e commitar direto.
- **Etapa 8 (Roadmap)**: spawnar equivalente ao `gsd-roadmapper` (não existe como agente — usar
  `general-purpose` com o mesmo prompt estruturado do workflow, apontando pra ler PROJECT.md +
  REQUIREMENTS.md + research/SUMMARY.md + config.json). Ordem de build já recomendada na SUMMARY.md
  seção 3: (1) Import LinkedIn → (2) Painel de consumo → (3) Status de assinatura → (4) Status de
  lifecycle de contato (por último, é o que toca o motor de envio). Isso deveria virar a base da
  estrutura de fases do ROADMAP.md. Precisa escrever ROADMAP.md + STATE.md + atualizar
  REQUIREMENTS.md com traceability. Como mode=yolo, auto-aprovar e commitar direto
  (`docs: create roadmap (N phases)`).
- **Etapa 9 (Done)**: apresentar resumo final com tabela de artefatos + "Next Up" apontando pra
  `/gsd:discuss-phase 1` ou `/gsd:plan-phase 1`.
- **Ainda não criado**: o ambiente isolado de verdade (projeto Supabase novo + branch `fase-2` +
  deploy Vercel separado) — isso é decisão/infra, não faz parte do fluxo `/gsd:new-project` em si;
  provavelmente acontece como parte da Etapa 8 (roadmap) ou como primeira tarefa de execução da
  Fase 1 do roadmap (setup de ambiente antes de codar features).
</remaining_work>

<decisions_made>

- Ambiente de trabalho pra Fase 2: projeto Supabase NOVO (isolado, dados de teste) + branch git
  `fase-2` + deploy Vercel separado com URL própria — decisão minha, delegada pelo usuário. Motivo:
  V1 em produção real (Clínica Hassum, 1190 contatos) não pode ser arriscada; usuário só quer abrir
  um link e ver visualmente se funciona, sem setup local.
- GSD (Get Shit Done) usado pra planejar isso, não plano ad-hoc na conversa — regra do projeto
  (`RULE-GSD-MANDATORY.md`) e porque é trabalho multi-sessão que precisa ser retomável.
- Config do GSD escolhida sem perguntar (mode=yolo, standard, sequencial, research/plan_check/
  verifier=true, balanced) — usuário vinha delegando decisões técnicas o tempo todo nesta conversa.
- Pesquisa de mercado real solicitada explicitamente pelo usuário (não só o doc de referência dele) —
  por isso rodei os 4 pesquisadores com WebSearch/WebFetch real em vez de só usar o que ele já tinha
  levantado.
- Decisão de arquitetura mais importante que saiu da pesquisa (documentada em SUMMARY.md e deve
  virar Key Decision no PROJECT.md quando eu voltar): status de contato vira DUAS colunas —
  `contacts.status` continua travando o motor de envio (zero mudança), nova `contacts.lifecycle_stage`
  separada pro mini-CRM (Novo/Ativo/Dormindo/VIP) — evita quebrar silenciosamente o `run-automations`
  existente.
</decisions_made>

<blockers>
Nenhum blocker real. Único ponto de atenção: os agentes especializados do GSD
(`gsd-codebase-mapper`, `gsd-project-researcher`, `gsd-research-synthesizer`, `gsd-roadmapper`) não
existem neste ambiente — continuar exige adaptar cada etapa pra usar `subagent_type="general-purpose"`
com prompt equivalente ao que o workflow original pede (funcionou bem até agora, só dá mais trabalho
de escrever o prompt na mão).
</blockers>

<context>
Conversa mais ampla: o usuário (Leonardo, dono do ZapFlow) está no meio de uma sessão que também
lidou com o site da Dra. Thaís Hassum (WordPress) e uma feature no ToqyApp — o ZapFlow é o terceiro
projeto tratado nesta mesma sessão. O pedido dele foi: "quero chegar o mais perto possível desses
[Bolten/HelenaCRM] e ser melhor que eles" — visão de longo prazo até "virar um programa igual esses"
pra depois investir em escalação comercial (Instagram da empresa, mais clientes). A Fase 2 é só o
primeiro passo formal dessa jornada, não o objetivo final.

Já existe um artifact publicado (fora do GSD, feito antes de decidir usar GSD formalmente) resumindo
V1 + roadmap Fase 2/3/4 em: https://claude.ai/code/artifact/6ab79085-0e99-404a-a193-eb9c789809c3 —
pode servir de referência rápida ao retomar, mas o SUMMARY.md da pesquisa real (mais grounded, com
pesquisa de concorrência de verdade) deve ter prioridade sobre esse artifact se algo divergir.
</context>

<next_action>
Ao retomar: ler este arquivo + `.planning/PROJECT.md` + `.planning/research/SUMMARY.md`, depois
continuar direto na Etapa 7 (Requirements) do workflow `/gsd:new-project`
(`.claude/get-shit-done/workflows/new-project.md`, a partir da seção "## 7. Define Requirements") —
sem precisar refazer nada das etapas 1-6. Como mode=yolo, gerar REQUIREMENTS.md e ROADMAP.md direto,
sem parar pra gate de aprovação, e avisar o usuário só ao final com o resumo completo.
</next_action>
