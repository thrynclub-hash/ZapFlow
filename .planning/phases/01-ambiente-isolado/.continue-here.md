---
phase: 01-ambiente-isolado
task: 0
total_tasks: 3
status: ready_to_execute
last_updated: 2026-07-06T04:50:54.964Z
---

<current_state>
`/gsd:new-project` (fase 2 do ZapFlow) e `/gsd:plan-phase 1` estão 100% concluídos e commitados.
Fase 1 (Ambiente Isolado) tem 3 planos escritos, revisados por plan-checker e corrigidos —
pronta para `/gsd:execute-phase 1`. Nenhuma task de execução foi iniciada ainda (nenhuma chamada
real de `create_project`/`vercel deploy`/etc. foi feita — tudo até aqui foi planejamento).
Usuário pediu para pausar aqui e trocar de projeto.
</current_state>

<completed_work>

- `/gsd:new-project` completo: PROJECT.md, config.json (mode=yolo, depth=standard), pesquisa de
  mercado (STACK/FEATURES/ARCHITECTURE/PITFALLS/SUMMARY), REQUIREMENTS.md (22 reqs, 22/22 mapeados),
  ROADMAP.md (5 fases). Commits: `6d69713`, `335fee0`, `f8f764f`, `a296421`, `4fff365`.
- Toolkit GSD restaurado em `.claude/get-shit-done/` (não existia no projeto — foi encontrado no
  cache npx do pacote `mega-brain-ai` e copiado pra cá). **Não está commitado** (untracked),
  igual ao padrão já usado neste projeto antes.
- Bug real corrigido no toolkit: `execGit` em `.claude/get-shit-done/bin/lib/core.cjs` usava
  escaping de shell estilo POSIX (aspas simples) que não funciona no `cmd.exe` (Windows) — trocado
  para `execFileSync` (sem shell). Correção local, não commitada no repo (fica só na cópia local
  do toolkit).
- `/gsd:plan-phase 1` completo (usuário pediu para pular `/gsd:discuss-phase`):
  - `01-RESEARCH.md` — pesquisa com inspeção AO VIVO via Supabase MCP e Vercel CLI (sem provisionar
    nada): descobriu que a produção real (`bhiggyigsrqfabqhutne`, org `mwkjwallfderomyllbta`) está
    estruturalmente fora do alcance das credenciais desta sessão (isolamento por credencial, não só
    disciplina); nenhum projeto Vercel "zapflow" visível no escopo autenticado; ordem exata de
    aplicação dos 16 arquivos SQL de schema; tabela `verify_jwt` das 8 edge functions; 9 pitfalls
    específicos com mitigação. Commit: `8919b1c`.
  - 3 planos escritos (`01-01-PLAN.md`, `01-02-PLAN.md`, `01-03-PLAN.md`), wave 1 = 01-01, wave 2 =
    01-02 + 01-03 em paralelo (ambos dependem só de 01-01). Commit: `d777aae`.
  - Plan-checker rodou e achou 2 issues reais, corrigidos diretamente por mim (sem novo round-trip
    de agente, por serem mecânicos): (1) `01-01` exigia a `service_role_key` do projeto novo pra
    agendar o cron, mas nenhuma tool MCP disponível retorna esse valor — virou item de `user_setup`
    e o plano passou de `autonomous: true` para `autonomous: false`; (2) ordem de aplicação dos
    arquivos 3/4 estava invertida (`supabase_automations.sql` tem no próprio cabeçalho a instrução
    de rodar DEPOIS de `supabase_security_fixes.sql` — corrigido). Também corrigi uma nota
    desatualizada em `01-02` ("7 commits pendentes" → hoje são 9, task agora usa contagem dinâmica
    em vez de número fixo). Commit: `dc93b2c`.
</completed_work>

<remaining_work>

- **Nada de planejamento pendente para a Fase 1** — está pronta pra executar.
- Próximo passo real: `/gsd:execute-phase 1`, que vai (via agentes `gsd-executor`/`gsd-verifier`
  adaptados como `general-purpose`, já que os subagentes especializados do GSD não existem neste
  ambiente):
  1. Rodar `01-01-PLAN.md` (wave 1): criar projeto Supabase `zapflow-fase2`, aplicar os 16 SQLs na
     ordem corrigida, habilitar `pg_cron`/`pg_net`, agendar cron do `run-automations`, deploy das
     8 edge functions. **Vai parar em algum ponto da Task 5 pedindo a `service_role_key`** do
     projeto novo (não existe até o projeto ser criado nesta mesma task — não dá pra antecipar).
  2. Rodar `01-02` e `01-03` em paralelo (wave 2): branch `fase-2` + deploy Vercel dedicado; e
     secrets/usuários Auth manuais + seed de dados fake.
- Depois da Fase 1 executada e verificada: `/gsd:discuss-phase 2` ou `/gsd:plan-phase 2`
  (Import de Leads do LinkedIn).
</remaining_work>

<decisions_made>

- Ambiente isolado = projeto Supabase **novo** (`zapflow-fase2`, não branching) + branch git
  `fase-2` + projeto Vercel **dedicado** (`zapflow-fase2`, não environment/preview dentro do
  projeto existente) — decisão já validada pela pesquisa contra o stack real.
- `contacts.status` (gate de envio) nunca é tocado pelo lifecycle da Fase 5; nova coluna
  `contacts.lifecycle_stage` separada.
- Ordem de build por risco: Import LinkedIn (Fase 2) → Painel de Consumo (Fase 3) → Status de
  Assinatura (Fase 4) → Lifecycle de Contato (Fase 5, por último — é a única que mexe perto do
  gate de envio).
- `service_role_key` do projeto Supabase novo precisa vir de Leonardo (dashboard) — nenhuma
  ferramenta MCP disponível nesta sessão a expõe, por design de segurança do Supabase.
</decisions_made>

<blockers>
Nenhum blocker real pendente. Pontos de atenção para quando a execução rodar:
- `01-01` vai precisar da `service_role_key` do projeto Supabase novo assim que ele for criado —
  Leonardo vai precisar pegá-la no Dashboard (Project Settings → API) e informar durante a execução.
- `01-03` precisa de: token de teste do Mercado Pago (conta de developer do Leonardo,
  mercadopago.com.br/developers) e criação manual de 2 usuários Supabase Auth via dashboard
  (nenhuma MCP tool cria usuários Auth).
- Continuam não existindo os subagentes especializados do GSD (`gsd-executor`, `gsd-verifier`,
  etc.) neste ambiente — `/gsd:execute-phase 1` vai precisar da mesma adaptação manual
  (`general-purpose` com prompt equivalente) usada em todas as etapas anteriores.
</blockers>

<context>
Sessão mais ampla: esta é uma continuação de uma sessão anterior que também tratou o site da
Dra. Thaís Hassum (WordPress) e uma feature no ToqyApp — o usuário está trocando de projeto agora
(saindo do ZapFlow), não abandonando o trabalho. Trabalho de "Fase 2" aqui = evoluir o ZapFlow pra
competir com Bolten/HelenaCRM (visão de longo prazo do Leonardo, dono do Marusso Produções).

Esta sessão também restaurou e corrigiu o toolkit GSD (`.claude/get-shit-done/`), que não é
commitado no repo (é tooling local, não artefato de projeto) — se uma sessão futura começar do
zero num checkout limpo do repo, `.claude/get-shit-done/` vai estar ausente de novo e vai
precisar ser restaurado a partir do cache npx do `mega-brain-ai` (ou reinstalado) antes de
qualquer comando `/gsd:*` funcionar. A correção do bug de `execGit` (POSIX quoting quebra no
Windows) também precisará ser reaplicada nesse cenário.
</context>

<next_action>
Ao retomar: ler este arquivo, depois `.planning/STATE.md` e `.planning/ROADMAP.md` pra reconfirmar
posição. Se `.claude/get-shit-done/` não existir mais (checkout limpo), restaurar primeiro (ver
<context> acima). Rodar `/gsd:execute-phase 1` para executar os 3 planos da Fase 1.
</next_action>
